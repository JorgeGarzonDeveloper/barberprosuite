[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --no-frozen-lockfile && npx prisma generate && npx tsc"

[deploy]
preDeployCommand = "npx prisma migrate deploy"
startCommand = "node dist/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
