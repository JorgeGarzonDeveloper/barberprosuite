[build]
builder = "NIXPACKS"
buildCommand = "npm install -g @nestjs/cli && pnpm install --no-frozen-lockfile && npx prisma generate && nest build"

[deploy]
preDeployCommand = "npx prisma migrate deploy"
startCommand = "node dist/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
